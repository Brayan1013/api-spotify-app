import { ExampleTrackPipe } from './example-track.pipe';

describe('ExampleTrackPipe', () => {
  it('create an instance', () => {
    const pipe = new ExampleTrackPipe();
    expect(pipe).toBeTruthy();
  });
});
